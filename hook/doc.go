// Package hook provides lifecycle hooks that are executed before and after
// a grpcannon load-test run, allowing callers to inject setup and teardown
// logic without modifying the core runner.
package hook
